<% include partials/header %>

<h1>This is shops page</h1>

<% shops.forEach( (shop) => { %>
    <div>
        <h4><%= shop.name%></h4>
        <img src="<%= shop.image %>"
    </div>
   
<% }); %>

<% include partials/footer %>